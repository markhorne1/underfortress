# Copilot Workspace Instructions

- Always format terminal commands in fenced bash code blocks.
- Prefer one command per fenced block for easy copy/run actions.
- When sharing multiple commands, present them as a short numbered sequence with one bash block per step.
